html
	body
		h1 Welcome to Test-Driven Architecture POC
		h2 Add products to cart
		form(action="/cart" method="post")
			each product in products
				button(name="product" value=`${product}` type="submit") #{product}

			ul
				each cartProduct in cartProducts
					li #{cartProduct.item} X #{cartProduct.quantity}